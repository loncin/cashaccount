"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
function default_1(filename, start = process.cwd()) {
    while (start !== "/" && !fs.existsSync(path.join(start, filename))) {
        start = path.dirname(start);
    }
    return start === "/"
        ? null
        : start;
}
exports.default = default_1;
