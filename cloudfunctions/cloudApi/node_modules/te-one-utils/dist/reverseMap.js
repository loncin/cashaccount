"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.reverseMap = void 0;
function reverseMap(targetObj) {
    let tempObj = {};
    let targetObjKeys = Object.keys(targetObj);
    targetObjKeys.forEach(keys => {
        targetObj[keys] && (tempObj[targetObj[keys]] = keys);
    });
    return tempObj;
}
exports.reverseMap = reverseMap;
